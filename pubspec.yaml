name: ml_kit_image_labeling_project
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'


# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_mlkit_image_labeling: ^0.10.0
  camera: ^0.10.5+9
  image_picker: ^1.1.0
  path_provider: ^2.1.3
  

  #STATE_MANAGMENT
  stacked: ^3.0.1
  stacked_services: ^1.0.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner:
  stacked_generator: ^1.3.2

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/ml/
  
